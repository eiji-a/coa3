<tr>
  <td>
    <%= link_to image_tag('unlink12.gif'), :action => 'unlink',
    :id => @content, :parent_id => parent.id, :child_id => @content.id %>
  </td>
  <td><%= link_to parent.title, :id => parent.id %></td>
  <td><%= link_to parent.folder.name, :controller => 'folders', :action => 'show', :id => parent.folder.id %></td>
  <td><%= parent.created_at.strftime("%Y-%m-%d") %></td>
</tr>
